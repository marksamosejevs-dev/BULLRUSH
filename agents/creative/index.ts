// Creative agent — Phase 2.
//
// Will generate UGC scripts, visual asset briefs, and Meta/TikTok campaign
// briefs (Creative rows) for a product moving toward BUILDING. Not
// implemented in Phase 1: creatives are entered by hand.

import { AgentResult, notConfigured } from "../types";

export async function runCreative(_opportunityId: string): Promise<AgentResult> {
  return notConfigured("Creative agent");
}
